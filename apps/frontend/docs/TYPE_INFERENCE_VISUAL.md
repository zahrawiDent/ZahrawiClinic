# 🎨 Type Inference Visual Guide

Visual diagrams to help understand how the automatic type inference system works.

## 📊 The Type Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     YOUR CODE                                        │
│  const todos = useCollection("todos")                               │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         │ TypeScript captures "todos" as literal type
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  TYPE HELPER                                         │
│  InferRecordType<"todos">                                           │
│                                                                      │
│  Is "todos" in CollectionRecords? → YES!                            │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         │ Lookup in mapping
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│              COLLECTION RECORDS (Lookup Table)                       │
│  export interface CollectionRecords {                               │
│    todos: TodoRecord      ← Found it!                               │
│    patients: PatientsRecord                                         │
│    users: UsersRecord                                               │
│  }                                                                   │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         │ Returns TodoRecord
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  RECORD DEFINITION                                   │
│  export interface TodoRecord extends BaseRecord {                   │
│    id: string                                                        │
│    created: string                                                   │
│    updated: string                                                   │
│    title: string       ← You get these fields!                      │
│    completed: boolean  ← With full autocomplete!                    │
│  }                                                                   │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         │ Final return type
                         ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    RESULT                                            │
│  todos.data = {                                                      │
│    items: TodoRecord[]     ← Fully typed!                           │
│    totalItems: number                                                │
│    page: number                                                      │
│    ...                                                               │
│  }                                                                   │
│                                                                      │
│  todos.data.items[0].title      ✅ Autocomplete works!              │
│  todos.data.items[0].completed  ✅ Type-safe!                       │
│  todos.data.items[0].invalid    ❌ Error! Doesn't exist             │
└─────────────────────────────────────────────────────────────────────┘
```

## 🔄 The Complete System

```
┌────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│                      pocketbase-types.ts                               │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 1. DEFINE RECORD TYPES                                           │ │
│  │                                                                   │ │
│  │ export interface TodoRecord extends BaseRecord {                 │ │
│  │   title: string                                                  │ │
│  │   completed: boolean                                             │ │
│  │ }                                                                 │ │
│  │                                                                   │ │
│  │ export interface PatientsRecord extends BaseRecord {             │ │
│  │   name: string                                                   │ │
│  │ }                                                                 │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 2. CREATE MAPPING (The Key!)                                     │ │
│  │                                                                   │ │
│  │ export interface CollectionRecords {                             │ │
│  │   todos: TodoRecord        ← "todos" → TodoRecord                │ │
│  │   patients: PatientsRecord ← "patients" → PatientsRecord         │ │
│  │   users: UsersRecord       ← "users" → UsersRecord               │ │
│  │ }                                                                 │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────┬───────────────────────────────────────────────┘
                          │
                          │ Import mapping
                          ↓
┌────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│                          queries.ts                                    │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 3. TYPE HELPERS (Translation Layer)                              │ │
│  │                                                                   │ │
│  │ type InferRecordType<K> =                                        │ │
│  │   K extends keyof CollectionRecords                              │ │
│  │     ? CollectionRecords[K]    ← Look up in mapping               │ │
│  │     : BaseRecord              ← Fallback for unknown             │ │
│  │                                                                   │ │
│  │ type CollectionName =                                            │ │
│  │   keyof CollectionRecords | (string & {})                        │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ 4. HOOKS (Use the helpers!)                                      │ │
│  │                                                                   │ │
│  │ export function useCollection<K extends CollectionName>(         │ │
│  │   collection: K                                                  │ │
│  │ ) {                                                               │ │
│  │   return useQuery(() => ({                                       │ │
│  │     queryFn: () => pb.getList<InferRecordType<K>>(...)          │ │
│  │                                ^^^^^^^^^^^^^^^^^^                 │ │
│  │                                Magic happens here!                │ │
│  │   }))                                                             │ │
│  │ }                                                                 │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────┬───────────────────────────────────────────────┘
                          │
                          │ Import and use
                          ↓
┌────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│                     Your Component                                     │
│                                                                         │
│  import { useCollection } from '@/lib/queries'                         │
│                                                                         │
│  function TodosPage() {                                                │
│    const todos = useCollection("todos")                                │
│           ↑                        ↑                                    │
│           │                        │                                    │
│           │                        └── TypeScript captures "todos"     │
│           │                            as literal type                 │
│           │                                                             │
│           └── Result is typed as:                                      │
│               { data: { items: TodoRecord[], ... } }                   │
│                                                                         │
│    return (                                                             │
│      <For each={todos.data?.items}>                                    │
│        {(todo) => (                                                    │
│          <div>                                                          │
│            {todo.title}      ← Autocomplete works! ✨                  │
│            {todo.completed}  ← Type-safe! ✨                           │
│          </div>                                                         │
│        )}                                                               │
│      </For>                                                             │
│    )                                                                    │
│  }                                                                      │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

## 🎯 Adding a New Collection (Visual)

```
Step 1: Define the Type
┌─────────────────────────────────────┐
│ pocketbase-types.ts                 │
│                                     │
│ export interface PostsRecord        │
│   extends BaseRecord {              │
│   title: string                     │
│   content: string                   │
│   published: boolean                │
│ }                                   │
└─────────────────────────────────────┘
              │
              │ Step 2: Add to Collection Table
              ↓
┌─────────────────────────────────────┐
│ pocketbase-types.ts                 │
│                                     │
│ interface AppCollections {          │
│   todos: TodoRecord                 │
│   patients: PatientsRecord          │
│   posts: PostsRecord  ← Add here!   │
│ }                                   │
│                                     │
│ // Automatically included:          │
│ export interface CollectionRecords  │
│   extends AuthCollections,          │
│           AppCollections {}         │
└─────────────────────────────────────┘
              │
              │ Step 3: Works Everywhere!
              ↓
┌─────────────────────────────────────┐
│ Your Component                      │
│                                     │
│ const posts = useCollection("posts")│
│ // ✅ Typed as PostsRecord[]        │
│                                     │
│ const create = useCreateRecord(     │
│   "posts"                           │
│ )                                   │
│ // ✅ Knows PostsRecord fields      │
│                                     │
│ useRealtimeCollection("posts")      │
│ // ✅ Syncs PostsRecord updates     │
└─────────────────────────────────────┘
```

## 🔍 Type Checking Flow

```
What TypeScript Does When You Write:
const todos = useCollection("todos")

┌──────────────────────────────────────┐
│ Step 1: Capture Literal Type         │
│                                      │
│ "todos" is not just type "string"   │
│ It's the literal type "todos"       │
│ (exact value preserved!)            │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Step 2: Generic Parameter            │
│                                      │
│ useCollection<"todos">               │
│ K = "todos"                          │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Step 3: Evaluate InferRecordType     │
│                                      │
│ InferRecordType<"todos">             │
│ → Is "todos" in CollectionRecords?  │
│ → YES! (extends keyof)              │
│ → Return CollectionRecords["todos"] │
│ → Which is TodoRecord               │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Step 4: Apply to Return Type         │
│                                      │
│ pb.getList<TodoRecord>()             │
│ → Returns ListResult<TodoRecord>    │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Step 5: Final Type                   │
│                                      │
│ todos: {                             │
│   data: {                            │
│     items: TodoRecord[]              │
│     totalItems: number               │
│     ...                              │
│   }                                  │
│   isLoading: boolean                 │
│   error: Error | null                │
│   ...                                │
│ }                                    │
└──────────────────────────────────────┘
```

## 🤔 Why Does This Work?

### The Key Ingredients:

1. **Literal Types**
   ```typescript
   const x = "todos"  // Type: "todos" (exact value!)
   let y = "todos"    // Type: string (generic)
   ```

2. **Conditional Types** (like if-else for types)
   ```typescript
   K extends keyof CollectionRecords 
     ? CollectionRecords[K]  // if true
     : BaseRecord            // if false
   ```

3. **Indexed Access** (lookup in object type)
   ```typescript
   CollectionRecords["todos"]  → TodoRecord
   CollectionRecords["patients"] → PatientsRecord
   ```

4. **Generic Type Parameters** (pass types as arguments)
   ```typescript
   useCollection<K>  // K is like a parameter
   InferRecordType<K>  // Use K to compute result
   ```

### Put Together:

```typescript
// You write this:
const todos = useCollection("todos")

// TypeScript sees:
useCollection<"todos">  // K = "todos" (literal!)

// Evaluates:
InferRecordType<"todos">  // Looks up "todos"

// Returns:
TodoRecord  // Found in mapping!

// Final type:
todos: CreateQueryResult<ListResult<TodoRecord>>
```

## 🎓 Learning Path

### Level 1: User
- Just add to `CollectionRecords`
- Use the hooks
- Get autocomplete!

### Level 2: Understanding
- Read this visual guide
- Understand the flow
- Know why it works

### Level 3: Mastery
- Read `TYPE_INFERENCE_GUIDE.md`
- Understand TypeScript features
- Create your own type utilities!

---

**Remember:** You don't need to understand all the TypeScript magic to use it!
The system is designed so you just:
1. Add your type to `CollectionRecords`
2. Start using it with full type safety! ✨
